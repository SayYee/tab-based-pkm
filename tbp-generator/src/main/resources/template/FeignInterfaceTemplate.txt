// 使用模版生成，请不要手动修改
package ${package};

import org.springframework.web.bind.annotation.*;

@RequestMapping("${rootMapping}")
public interface ${functionName} extends ${functionInfo.functionName} {
<%
  for(methodInfo in functionInfo.methodInfos){
    println("@GetMapping(\"/" + methodInfo.methodName + "\")");
    println("@Override");
    print(methodInfo.returnType);
    print(" ");
    print(methodInfo.methodName);
    print(" ");
    print("(");
    for (fieldInfo in methodInfo.fieldInfos) {
        if (!fieldInfoLP.first) {
            print(", ");
        }
        print("@RequestParam(\"" + fieldInfo.paramName + "\")");
        print(fieldInfo.paramType);
        print(" ");
        print(fieldInfo.paramName);
    }
    print(")");
    for (exception in methodInfo.exceptions) {
        if (exceptionLP.first) {
            print(" throws ");
        }
        print(exception);
        if (!exceptionLP.last) {
            print(", ");
        }
    }
    println(";");
  }
%>
}